import styled from 'styled-components';
import { color } from '../../../constants/color';
import { CommonButton } from '../../Common/Button/Button.styled';

export const ReadmoreButton = styled(CommonButton)`
  display: block;
  width: 100%;
  margin: 2rem 0;
  padding: 0.75rem 0;
  font-size: 1rem;
  font-weight: bold;
`;
